
<footer>
  <div class="footer-container">
    <div class="footer-column-wrapper">
        <div class="footer-column-inner">
          <ul class= "footer-column-categories">
            <% Category.all.each_with_index do |category, i| %>
              <% next if i.zero? %>
                <li class="footer-column-category-parts">
                <%= link_to apps_category_search_path(category_id: category.id) do%>
                  <div class="footer-category-link-parts">
                    <p class="footer-category-name"><%= category.name %></p>
                  </div>
                <% end %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>

    <div class="text-center">
      <p>Copyright © Ujihara Miyu</p>
    </div>
  </div>
</footer>
 
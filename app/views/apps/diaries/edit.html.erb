<div class="diaries-new-wrapper">
  <div class="diaries-sidebar">
    <%= render "apps/users/apps_sidemenubar"%>
  </div>
  <%= form_with model: @diary, url: apps_diary_path(@diary), method: :put, local: true do |f| %>
    <%= render "apps/diaries/form",  f: f %>
  <% end %>
</div>
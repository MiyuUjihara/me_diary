
<article class="admin-column-table">
  <div class="column-table-inner">
    <h2 class="table-title">List of columns</h2>
    <div class="table-container">
      <table class="table table-striped table-hover">
          <thead>
            <tr class="table-column">
              <th scope="col" class="val">id</th>
              <th scope="col" class="val">user_name</th>
              <th scope="col" class="val">status</th>
              <th scope="col" class="val">category</th>
              <th scope="col" class="val">title</th>
              <th scope="col" class="val">thumbnail_image</th>
              <th scope="col" class="val"></th>
            </tr>
          </thead>
          <tbody>
            <% @columns.each do |column| %>
              <tr>
                <th scope="row" class="val val2"><%= column.id %></th>
                <td class="val"><%= column.admin_user.name %></td>
                <td class="val"><%= column.status %></td>
                <td class="val"><%= column.category.name %></td>
                <td class="val"><%= column.title %></td>
                <td class="val"><%= image_tag column.image.url, size: "90x60" %></td>
                <td class="val">
                  <%= link_to "edit", edit_admin_column_path(column), class: "column-table-btn"%><br>
                  <%= link_to "delete", admin_column_path(column), method: :delete, data: { confirm: "【確認】コラムを削除してもよろしいですか？" }, class: "column-table-btn"%></td>
                </td>
              </tr>
            <% end %>
          </tbody>
      </table>
    </div>
  </div>

  <div class="column-table-inner">
    <h2 class="table-title">List of Todos</h2>
    <div class="table-container">
      <table class="table table-striped table-hover">
          <thead>
            <tr class="table-column">
              <th scope="col" class="val">id</th>
              <th scope="col" class="val">user_name</th>
              <th scope="col" class="val">status</th>
              <th scope="col" class="val">category</th>
              <th scope="col" class="val">title</th>
              <th scope="col" class="val">thumbnail_image</th>
              <th scope="col" class="val"></th>
            </tr>
          </thead>
          <tbody>
            <% @todos.each do |todo| %>
              <tr>
                <th scope="row" class="val val2"><%= todo.id %></th>
                <td class="val"><%= todo.admin_user.name %></td>
                <td class="val"><%= todo.status %></td>
                <td class="val"><%= todo.category.name %></td>
                <td class="val"><%= todo.title %></td>
                <td class="val"><%= image_tag todo.image.url, size: "90x60" %></td>
                <td class="val">
                  <%= link_to "edit", edit_admin_todo_path(todo), class: "column-table-btn"%><br>
                  <%= link_to "delete", admin_todo_path(todo), method: :delete,  data: { confirm: "【確認】ToDoToDoを削除してもよろしいですか？" }, class: "column-table-btn"%></td>
                </td>
              </tr>
            <% end %>
          </tbody>
      </table>
    </div>
  </div>


</article>



